import { NoteStateDirective } from './note-state.directive';

describe('NoteStateDirective', () => {
  it('should create an instance', () => {
    const directive = new NoteStateDirective();
    expect(directive).toBeTruthy();
  });
});
